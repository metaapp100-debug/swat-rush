{
  "name": "swat-rush",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "deploy": "npm run build && npx --yes gh-pages -d dist --nojekyll"
  },
  "dependencies": {
    "phaser": "^3.88.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
